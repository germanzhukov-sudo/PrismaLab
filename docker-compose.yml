services:
  prismalab:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - prismalab_data:/app/data
    # Обходим блокировку одного IP из DNS-пула api.telegram.org на RU-провайдере.
    # DNS выдаёт 149.154.166.110 (заблокирован), остальные IP работают.
    extra_hosts:
      - "api.telegram.org:149.154.167.220"
    environment:
      # SQLite в volume; при DATABASE_URL используется PostgreSQL (Supabase)
      - PRISMALAB_DB_PATH=/app/data/prismalab.db
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # Опционально: лимиты при росте нагрузки
    # deploy:
    #   resources:
    #     limits:
    #       memory: 1G
    #     reservations:
    #       memory: 256M

  support:
    build: .
    restart: unless-stopped
    env_file: .env
    extra_hosts:
      - "api.telegram.org:149.154.167.220"
    command: ["python", "-m", "prismalab.support_bot"]

volumes:
  prismalab_data:

## PrismaLab

Telegram-бот: пользователь отправляет фото → выбирает стиль → получает стилизованную картинку.

### Установка

```bash
pip3 install -r requirements.txt
```

### Настройка

Самый простой способ: запусти скрипт, он сам создаст `.env` и попросит токены:

```bash
python3 setup_env.py
```

Либо создай файл `.env` рядом с `README.md` вручную (см. также `prismalab/env.prismalab.example`).

**База данных (Supabase):** чтобы хранить пользователей и генерации в облаке, создай проект в [Supabase](https://supabase.com), в настройках проекта открой **Project Settings → Database → Connection string (URI)**. Скопируй строку, подставь пароль от БД (тот, что задавал при создании проекта) и добавь в `.env`:

```env
DATABASE_URL=postgresql://postgres.ПРОЕКТ:ТВОЙ_ПАРОЛЬ@...pooler.supabase.com:6543/postgres
```

Если не задавать `DATABASE_URL`, бот будет использовать локальный файл `prismalab.db` (SQLite).

### Деплой на сервер (Docker + Supabase)

1. **Supabase:** в проекте Supabase открой **Project Settings → Database → Connection string (URI)**. Скопируй URI (удобно Transaction pooler, порт 6543), подставь пароль БД.
2. **Локально** в `.env` добавь строку:  
   `DATABASE_URL=postgresql://...` (полная строка из Supabase).
3. Из корня репозитория:  
   `./deploy.sh` — проект скопируется на сервер, контейнер соберётся и запустится.  
   Остановить бота на сервере: `./deploy.sh stop`.
4. Логи на сервере:  
   `ssh root@194.87.133.7 "cd /root/PrismaLab && docker compose logs -f"`.  
   В логах должно быть: `База данных: PostgreSQL (Supabase)`.

Не запускай бота локально и на сервере одновременно — иначе будет Conflict (один токен = один инстанс).

### Движки

- **`instantid`**: лучший режим для сохранения лица (дефолт).
- **`flux_ultra_swap`**: *вау-картинка* (FLUX Ultra) + **фиксация лица (face swap)** + **upscale**. Самый близкий к “мама не отличит”, но самый дорогой (3 прогона).
- **`flux_ultra`**: картинка “вау”, но лицо может “плыть”.
- **`flux`**: экспериментальный.
- **`sdxl` / `photomaker`**: запасные режимы.

### Максимальная точность (10 фото)

В боте есть кнопка **«🎯 Максимальная точность (10 фото)»**:
- ты отправляешь 10 фото (разные ракурсы)
- бот запускает обучение персональной модели (LoRA) через Replicate
- после обучения стилизация становится заметно точнее по чертам лица

Для обучения нужно указать username Replicate:

```env
PRISMALAB_REPLICATE_OWNER=your_replicate_username
```

И hardware для автосоздания destination модели (обязательно для Replicate API):

```env
PRISMALAB_REPLICATE_HARDWARE=gpu-l40s
```

Опционально:

```env
PRISMALAB_LORA_STEPS=1000
PRISMALAB_LORA_RANK=16
PRISMALAB_LORA_TRAINER_VERSION=  # если хочешь зафиксировать версию ostris/flux-dev-lora-trainer
```

Для `flux_ultra_swap` можно переопределить модели пайплайна:

```env
PRISMALAB_FACESWAP_MODEL_VERSION=7d356d4bcc723129e33b94b41cce7ab673d45166822fe3e8cce51df7afb2fc21
PRISMALAB_UPSCALE_MODEL_VERSION=e84596a7e0bd288ffc063ec00d224fa20b70152f1ce4aa14db21bc1f0bff00b6
PRISMALAB_UPSCALE_SCALE=2
PRISMALAB_UPSCALE_FACE_ENHANCE=true
```

### Запуск

Основной бот:

```bash
python3 -m prismalab.bot
```

Бот поддержки (пересылает сообщения админу; админ отвечает reply — ответ идёт пользователю):

```bash
python3 -m prismalab.support_bot
```

В `.env` укажи `PRISMALAB_SUPPORT_BOT_TOKEN` и `PRISMALAB_SUPPORT_ADMIN_ID`.

### Важно про качество

- **Вход**: если отправлять как “Фото”, Telegram сжимает. Для максимального качества отправляй как **Файл/Документ**.
- **Выход**: бот отправляет результат **как файл (без сжатия)**, чтобы качество не терялось.
